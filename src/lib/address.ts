export type AddressSuggestion = {
  id: string;
  label: string;
  street: string;
  houseNumber: string;
  postalCode: string;
  city: string;
  country: string;
};

export type AddressValue = {
  street: string;
  houseNumber: string;
  postalCode: string;
  city: string;
  country: string;
};
